routes:
  /signup/: members/signup
  /signin/: members/signin
  /account/: members/account
  /offline/: offline
  /recommendations/:
    template: recommend
    # data: page.recommendations # When active data will be taken from the  "/recommendations/" page
  /stacks/:
    controller: channel
    template: vault
    filter: featured:false+tag:-calendar

collections:
  /:
    permalink: /{slug}/
    template: index

taxonomies:
  tag: /tag/{slug}/
  author: /author/{slug}/
