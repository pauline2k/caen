{{!< default}}

<div class="container-fluid wrapper">
  <section class="text-center no-img">
    <div class="geometric-bg"></div>
    <div class="stacks-header__content">
      <h1 class="stacks-header__title">The ORB Stacks</h1>
      {{#if description}}
        <div class="tag-header__description">{{description}}</div>
      {{/if}}
      <div class="tag-header__counter">
        {{plural ../pagination.total empty=(t 'No posts') singular=(t '1 post') plural=(t '% posts')}}
      </div>
    </div>
  </section>

  {{!-- The main content/post by the author --}}
  {{#if posts}}
    <div class="row">
      <div class="col-xs-12">
        <section class="section all-posts">
          <div class="orb-rows" role="table" aria-label="Articles">
              {{#foreach posts}}
                {{!-- Collect tag slugs to filter on --}}
                <article
                  class="orb-row"
                  role="row"
                  data-tags="{{#foreach tags}}{{slug}}{{^last}} {{/last}}{{/foreach}}"
                >
                  <div class="c c--section" role="cell">
                    <div>{{tags}}</div>
                    <div>{{date format="M/D/YY"}}</div>
                  </div>

                  <div class="c c--title" role="cell">
                    <a class="orb-title-link" href="{{url}}">{{title}}</a>
                  </div>

                  <div class="c c--dek" role="cell">
                    {{#if meta_description}}
                      {{meta_description}}
                    {{else}}
                    {{#if custom_excerpt}}
                      {{custom_excerpt}}
                    {{else}}
                      {{excerpt words="25"}}…
                    {{/if}}
                    {{/if}}
                  </div>

                  <div class="c c--author" role="cell">
                    {{^has author='\O/'}}
                      {{primary_author.name}}
                    {{/has}}
                  </div>
                </article>
              {{/foreach}}
          </div>
        </section>
      </div>
    </div>

    {{pagination}}
  {{/if}}
</div>