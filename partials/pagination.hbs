<nav class="pagination">

  {{#if prev}}
    <a href="{{page_url prev}}">← Newer |</a>
  {{/if}}

  <span>Page {{page}} of {{pages}}</span>

  {{#if next}}
    <a href="{{page_url next}}">| Older →</a>
  {{/if}}

</nav>