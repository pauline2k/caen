{{!-- Content preview --}}
<div class="post-sneak-peek{{#if fading}} fading{{/if}}">
  {{{html}}}
</div>

{{!-- Post Access CTA --}}
<div class="post-access-cta p-lg text-center {{visibility}}">
  <h3 class="paid">{{t "This post is for paid members only"}}</h3>
  <h3 class="members">{{t "This post is for members only"}}</h3>
  <a class="btn btn--brand btn--rounded m-b-lg" href="/signup/"{{#if @custom.use_portal_links}} data-portal="signup"{{/if}}>{{t "Subscribe"}}</a>
  <div class="small">{{t "Already have an account?"}} <a class="post-access-alt" href="/signin/"{{#if @custom.use_portal_links}} data-portal="signin"{{/if}}>{{t "Sign in"}}</a></div>
</div>